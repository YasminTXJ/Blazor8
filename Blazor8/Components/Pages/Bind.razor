@page "/bind"
@rendermode InteractiveServer
@*
    rendermode:
        - None (sem iteração) : Vazio
        - Server  : InteractiveServer
        - WebAssembly : InteractiveWebAssembly
        - Auto (Server -> WebAssembly)  : InteractiveAuto
*@
<h3>Bind</h3>

<div class="mt-3">
@**
    Bind - Onidirecional - só le a informação e mostra - {get; }
**@
<p>@Texto</p>
</div>

<div class="mt-3">
    @*
    Bind - Bidirecional - obtem e atriui o dados  - {get; set;}
    *@

    <input  @bind="Texto" class="form-control"/> @* So altera quando tiro o foco pois usa o onchange*@
</div>
<div class="mt-3">
    <input @bind="Texto" @bind:event="oninput" class="form-control" /> @*as mudanças aparecem automaticamente*@
     
</div>
<div>
    <input @bind-value="Texto" @bind-value:event="oninput" class="form-control"/>
</div>

<div class="mt-3">
    <input @bind:get="Texto"  @bind:set="(value)=>{Texto = value!;}" class="form-control" /> 

</div>
@code {
    private string Texto { get; set; } = "Eu sou um texto!";
}
